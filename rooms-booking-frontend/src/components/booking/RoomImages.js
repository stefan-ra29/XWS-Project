import React from 'react'

export default function RoomImages(props) {

    const images = localStorage.getItem("roomImages");

    console.log(images);

  return (
    <div>
         a
    </div>
  )
}
